version: '3.9'
services:
  api:
    container_name: bank_ddd_prototype_api
    image: node:14.18-alpine
    working_dir: /app
    volumes:
      - '.:/app'
    env_file: .env
    ports:
      - ${API_PORT}:${API_PORT}
    entrypoint: ./start.sh
    depends_on:
      - "db"
  
  db:
    container_name: bank_ddd_prototype_db
    image: postgres
    restart: always
    volumes:
      - /private/var/lib/postgresql:/var/lib/postgresql
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: pguser
      POSTGRES_PASSWORD: pguser
